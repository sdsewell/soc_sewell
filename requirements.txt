# WindCube FPI Pipeline — Python dependencies
# Pin versions once the full pipeline is implemented.
# Install into an active virtual environment: pip install -r requirements.txt

numpy
scipy
matplotlib
pandas
astropy
pytest
